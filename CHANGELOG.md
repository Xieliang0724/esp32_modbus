# Changelog

本项目的版本记录。版本号格式：`v主.次.修订`

- **主版本**：重大变更/不兼容改动
- **次版本**：新功能
- **修订版本**：bug 修复/小优化

> 📌 本工程由 **esp32c5_web_provision v1.2.0** 全量复制新建，版本号重新从 **v1.0.0** 计数。v1.1.0 起从"RTU↔TCP 网关"升级为"ESP32 本体 Modbus TCP 从站"。下方 v1.0.0~v1.2.0 记录为基版本（esp32c5_web_provision）的既有历史，保留备查。

## [v1.3.1] - 2026-08-17

### 🔧 修正：DI4/DI5 改为实时急停 IO 状态（不锁存）

- 离散输入 `0x1004/0x1005`（DI4/DI5）从"锁存"改为**实时触点状态**：随急停按下/松开实时变化（1=闭合正常，0=按下/断开）
- **锁存仅保留在输入寄存器 `0x3000`**（设备急停状态：任一路触发→0 锁存，急停松开+按复位才恢复 1）
- LED 红灯闪烁仍以 0x3000 锁存状态为准
- 文档与工具标注同步更新

## [v1.3.0] - 2026-08-17

### ✨ 新增：设备急停状态寄存器 + LED 红灯闪烁联动

- **输入寄存器重新分配**（FC04，0x3000 起扩展为 8 个）：
  - `0x3000`：**设备急停状态**（1=系统正常，0=急停触发，锁存，需复位解除）
  - `0x3001/0x3002`：急停1/急停2 **实时 IO 状态**（不经锁存，直接读触点）
  - `0x3003`：复位按钮实时状态
  - `0x3004~0x3007`：固件版本 / WiFi 状态 / 运行秒数（设备信息顺延至此处）
- **离散输入 DI 地址不变**（0x1000-0x1006）
- **LED 联动**：急停触发（任一路锁存）→ 板载 RGB **红色闪烁**（300ms 周期），覆盖网络状态色；解除锁存后恢复（联网=绿 / 未联网=橙）
- 新增 `mb_device_estop_active()` 接口 + `app_main` 急停监控任务（300ms 轮询）
- **修复**：FC04 读输入寄存器区域大小随 `MB_MAX_INREG` 扩展（原硬编码 4 导致读 0x3000+ 越界返回异常 0x02）
- **工具**：预设改为"读急停状态+设备信息"（0x3000 起 8 个），寄存器语义标注同步更新

## [v1.1.0] - 2026-08-16

### ✨ 新增：ESP32 本体 Modbus TCP 从站

- **架构变更**：从"RTU↔TCP 网关（透传 GD32）"升级为 **ESP32 本体作为 Modbus TCP 从站**，寄存器直接映射到本机外设；UART1 透传逻辑（UART 驱动、RTU 组帧、CRC16、rx 任务）**彻底移除**，UART1 引脚释放
- **寄存器映射表**（见 README）：
  - 线圈 `0x0000`（FC01/05/0F）：DO0-DO3，GPIO 数字输出，上电默认断开
  - 离散输入 `0x1000`（FC02）：DI0-DI6（通用输入 4 路 + 急停 2 路 + 复位按钮 1 路）
  - 输入寄存器 `0x3000`（FC04）：固件版本 / WiFi 状态 / 运行秒数
  - 保持寄存器 `0x4000`（FC03/06/10）：用户参数 4 字（RAM 暂存）
- **完整功能码**：01/02/03/04/05/06/0F/10 + 标准异常码（01/02/03）
- **急停安全回路**（DI4/DI5，常闭 NC）：
  - 正常闭合 = 寄存器 1，按下 = 寄存器 0 并**锁存**（松开不回 1）
  - 解除条件：急停已松开 + 按一下复位按钮（DI6，上升沿，一次解除两路）
  - 只上报状态，不联动 DO
- **外设层**：GPIO 引脚全部在 menuconfig（`Modbus Slave Configuration`）配置，默认 DI=GPIO4-7、DO=GPIO23-26、急停1/2=GPIO8/10、复位=GPIO15（按开发板排针调整，避开 USB 13/14、串口 11/12、RGB 27、下载 28、BOOT 9；IO16-22 未引出排针），`-1` = 禁用该路
- **保留**：TCP/TLS 双监听（502/802）、客户端 IP 白名单、配网页高级设置配置
- **网页**：高级设置表单与 `/api/gw` 接口移除 RTU 波特率/TX/RX 字段，改为从站说明
- **新建模块**：`main/mb_device.[ch]`（寄存器模型 + 功能码处理 + GPIO 外设层 + 急停锁存）

## [v1.0.0] - 2026-08-16

### 🆕 项目建立

- 基于 esp32c5_web_provision v1.2.0 全量复制（源码 + sdkconfig + managed_components + TLS 证书），项目名改为 `esp32_modbus`
- Wi-Fi 配网、失联兜底、Modbus RTU↔TCP 网关（含 TLS）、RGB 状态灯等能力与基版本完全一致
- 后续 Modbus 增强功能在此版本基础上迭代

## [v1.2.0] - 2026-08-16

### ✨ 新增：AP 兜底延迟可配置

- **需求背景**：ESP32-C5 单射频，STA 连接/扫描时 AP 信号弱；且板子无复位按钮，AP 和 STA 绝不能同时失联
- **新逻辑**：STA 断开后不立即开关 AP，而是**连续断开超过 N 秒**（默认 15s）才自动开启 AP 兜底
  - 短暂断网（< N 秒）重连成功 → AP 不动作（避免射频来回跳变）
  - 长时间断开（≥ N 秒）→ 自动开启热点，随时可重新配网
- **网页可配**：配网表单新增"断网后开启热点兜底延迟（秒）"，范围 0~3600，0 = 立即开启，随配置持久化（NVS）

## [v1.1.4] - 2026-08-16

### 🔧 修复

- 联网成功后重置重试计数（避免重试次数跨连接周期累计，导致下次断网重试预算不足）

## [v1.1.3] - 2026-08-16

### 🔴 关键修复：AP/STA 不再同时失联

- **问题**：设备联网成功后（AP 已关闭、纯 STA 模式），一旦 STA 断开，状态机未切回 CONNECTING，导致重试逻辑直接返回 —— 既不重连也不重开 AP，设备永久失联（无复位按钮的板子只能拔电）
- **修复**：
  1. STA 断开时切回 CONNECTING 状态，让重试真正运行
  2. 重试期间保证 AP 可用（兜底不变量："未联网时 AP 必须在"）
  3. 重试耗尽后回退配网模式（AP 开启）
- **保证**：任何时刻 AP 和 STA 至少有一个可用

## [v1.1.2] - 2026-08-16

### 🔧 修复

- TLS 客户端任务栈 6144 → 8192（避免 mbedTLS 握手栈溢出）
- 修复客户端槽位泄漏（malloc/任务创建失败时未归还槽位，多次失败后耗尽导致拒绝所有连接）
- 客户端槽位分配互斥锁（明文/TLS 双监听并发安全）

## [v1.1.1] - 2026-08-16

### ✨ 新增

- **证书下载接口**：`GET /api/cert` 直接下载设备 TLS 证书（PEM），客户端无需从仓库/项目里翻文件，连接设备即可获取并信任

### 🔒 安全

- 私钥 `main/certs/server_key.pem` 移出公开仓库（加入 `.gitignore`），仅保留在本机用于编译

## [v1.1.0] - 2026-08-16

### ✨ 新增：Modbus TCP over TLS（单向验证）

- **TLS 加密监听**：新增 TLS 端口（默认 802，Modbus Security 标准），与明文 502 端口共存
- **单向 TLS**：客户端验证设备身份（服务端证书），设备不验证客户端
- **固件内置证书**：自签名服务器证书（CN=`esp32c5.local`，10 年有效）随固件内置；可通过 `main/certs/` 替换为自己的 CA 证书
- **网页配置**：高级设置新增"启用 TLS + TLS 端口"配置项，与明文端口冲突校验
- **协议一致**：TLS 连接同样走 RTU↔TCP 协议转换、CRC16 校验、IP 白名单、事务串行化

### 🔧 修复

- 修复 mbedTLS 解析嵌入式 PEM 证书失败（缓冲区需 null 结尾）
- 客户端槽位分配增加互斥锁（明文/TLS 双监听并发安全）

## [v1.0.0] - 2026-08-15

> ⚠️ **基础版本说明**：本版本为 **v1.0.0 基础版本**，**Modbus TCP 不带 TLS 验证功能**（明文 TCP，端口 502）。TLS 加密将在后续版本（v1.1.0+）提供；适用于可信局域网，跨公网/不可信网络请使用支持 TLS 的版本。

### ✨ 功能特性

- **Web 配网**：SoftAP 热点 + 网页配置（`http://192.168.4.1`），支持双频（2.4G/5G）扫描、SSID 下拉选择、隐藏网络手动输入
- **IP 配置**：DHCP 自动获取 / 静态 IP（IP、掩码、网关、DNS 可自定义）
- **SoftAP 管理**：可选"联网成功后关闭热点"；网页提供"开启/关闭热点"按钮和"一键进入配网模式"按钮
- **RGB 状态灯**（GPIO27 WS2812）：橙色=未联网、蓝色=有设备连热点、绿色=联网成功
- **Modbus RTU↔TCP 网关**（UART1）：协议转换（MBAP↔RTU+CRC16），支持本地端口、RTU 波特率、UART1 TX/RX GPIO、客户端 IP 白名单配置
- **mDNS**：局域网内可通过 `http://esp32c5.local` 访问
- **网络状态面板**：STA 状态/SSID/IP/网关/掩码/DNS/信号强度 + AP 状态/名称/客户端数/固件版本

### 🛡️ 可靠性设计

- **防失联保护**：SoftAP 仅在联网成功后关闭；连接中/配网模式热点始终可用
- **自动回退**：连接失败自动重试（可配次数），耗尽后回到配网模式
- **配置持久化**：NVS 存储，断电重启自动重连
- **长按 BOOT 复位**：清除配置并重新配网

### 🔧 其他

- 基于 ESP-IDF v6.0.1（ESP32-C5），8MB 闪存（N8R4）
- 固件内置版本号（网页/API/日志可见）
